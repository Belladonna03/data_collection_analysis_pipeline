# Configuration examples

- **`../config.yaml`** (repo root) — default **health forum** text classification with four labels from `annotation.labels`.
- **`safety_classification.example.yaml`** — snippet for the legacy **moderation** scenario (`task_name: safety_classification` without `labels`).
